global:
  # TTFT effects are sub-second; the default 15s scrape interval would alias
  # right over them.
  scrape_interval: 1s

scrape_configs:
  - job_name: kvroute
    static_configs:
      - targets: ["host.docker.internal:8000"]
